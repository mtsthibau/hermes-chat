PRODUCTION=false
LOCAL=true
DOMAIN=estacao4.hermes.radio
GATEWAY=true
PATH_URL_BACKEND=https://192.168.1.51/api
NEXT_PUBLIC_APP_TIME_ZONE=UTC
# Path to the PMTiles file served by the hermes-gps app.
# Relative path → served from /public/. Absolute URL → any HTTP source on the local network.
NEXT_PUBLIC_PMTILES_URL=/brazil.pmtiles